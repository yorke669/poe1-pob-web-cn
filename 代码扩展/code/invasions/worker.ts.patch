diff --git a/packages/driver/src/js/worker.ts b/packages/driver/src/js/worker.ts
index d4b3f07..92f9382 100644
--- a/packages/driver/src/js/worker.ts
+++ b/packages/driver/src/js/worker.ts
@@ -54,6 +54,8 @@ type Imports = {
   start: () => void;
   loadBuildFromCode: (code: string) => number;
   getBuildCode: () => string;
+  getBuildStats: () => string;
+  getItemCompareStats: (slotName: string, itemRaw: string) => string;
   onFrame: () => void;
   sentryTestCrash: () => void;
   onKeyUp: (name: string, doubleClick: number) => void;
@@ -260,6 +262,22 @@ export class DriverWorker {
     return code;
   }
 
+  async getBuildStats(): Promise<string> {
+    const stats = this.imports?.getBuildStats();
+    if (!stats) {
+      throw new Error("getBuildStats failed");
+    }
+    return stats;
+  }
+
+  async getItemCompareStats(slotName: string, itemRaw = ""): Promise<string> {
+    const stats = this.imports?.getItemCompareStats(slotName, itemRaw);
+    if (!stats) {
+      throw new Error("getItemCompareStats failed");
+    }
+    return stats;
+  }
+
   setLayerVisible(layer: number, sublayer: number, visible: boolean) {
     this.renderer?.setLayerVisible(layer, sublayer, visible);
     this.invalidate();
@@ -323,6 +341,8 @@ export class DriverWorker {
       start: module.cwrap("start", "number", []),
       loadBuildFromCode: module.cwrap("load_build_from_code", "number", ["string"]),
       getBuildCode: module.cwrap("get_build_code", "string", []),
+      getBuildStats: module.cwrap("get_build_stats", "string", []),
+      getItemCompareStats: module.cwrap("get_item_compare_stats", "string", ["string", "string"]),
       onFrame: module.cwrap("on_frame", "number", []),
       sentryTestCrash: module.cwrap("sentry_test_crash", null, []),
       onKeyUp: module.cwrap("on_key_up", "number", ["string", "number"]),
