diff --git a/packages/packer/src/pack.ts b/packages/packer/src/pack.ts
index ac8eaba..4f1ef1a 100644
--- a/packages/packer/src/pack.ts
+++ b/packages/packer/src/pack.ts
@@ -19,6 +19,7 @@ if (!isGame(game)) throw new Error(`Unsupported game: ${game}`);
 if (mode !== undefined && mode !== "clone") throw new Error(`Unsupported mode: ${mode}`);
 
 const clone = mode === "clone";
+const workspaceRoot = resolve(dirname(fromFileUrl(import.meta.url)), "../../..");
 const def = gameData[game];
 const buildDir = `build/${game}/${tag}`;
 const r2Dir = `r2/games/${game}/versions/${tag}`;
@@ -95,6 +96,19 @@ for await (const entry of walk(luaPath, { includeDirs: false, exts: [".lua"] }))
   zip.addFile(`lua/${relPath}`, Buffer.from(await Deno.readFile(entry.path)));
 }
 
+// Inject pob-web owned translation data (generated by tools/translate/sync-from-poecharm.py).
+// Placed under lua/ so boot.lua can `require "translate_manifest"` and the individual modules.
+// Skipped if the directory is absent.
+const translateDir = `${workspaceRoot}/packages/driver/translate`;
+try {
+  for await (const entry of walk(translateDir, { includeDirs: false, exts: [".lua"] })) {
+    const relPath = relative(translateDir, entry.path).replaceAll("\\", "/");
+    zip.addFile(`lua/${relPath}`, Buffer.from(await Deno.readFile(entry.path)));
+  }
+} catch (error) {
+  if (!(error instanceof Deno.errors.NotFound)) throw error;
+}
+
 zip.addFile(".image.tsv", Buffer.from(imageIndex.join("\n")));
 const manifest = (await Deno.readTextFile(`${repoDir}/manifest.xml`)).replace(
   /<Version number="([0-9.]+)" \/>/,
@@ -126,6 +140,8 @@ async function packInputHash(): Promise<string> {
     "packages/packer/deno.json",
     "packages/packer/src",
   ];
+  const translateDir = join(workspaceRoot, "packages/driver/translate");
+  if (await exists(translateDir)) inputs.push("packages/driver/translate");
   const files: string[] = [];
   for (const input of inputs) {
     const target = join(workspaceRoot, input);
